import { initUserMenu } from '../../src/header/userMenu.js';
import { initTheme } from '../../src/theme/theme.js';

// ── User menu + Theme ──────────────────────────────────────
initUserMenu();
initTheme();
